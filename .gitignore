# dependencies
node_modules/

# SQLite database + WAL/SHM sidecar files (regenerated by `npm run ingest`)
backend/data/*.db
backend/data/*.db-wal
backend/data/*.db-shm

# dataset files (large; regenerate via fetch_wikipedia.js or synthetic generator)
backend/data/*.tsv

# logs / env
*.log
.env
